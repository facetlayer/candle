{
  "name": "@facetlayer/candle-tests",
  "version": "0.13.3",
  "private": true,
  "license": "MIT",
  "type": "module",
  "packageManager": "pnpm@10.33.2",
  "description": "Vitest acceptance suite for the Rust-based Candle CLI. The Candle implementation lives in ./rust; this package exists only to run the end-to-end tests in ./test against the compiled binary.",
  "scripts": {
    "test": "cargo build --release --manifest-path rust/Cargo.toml && CANDLE_TEST_TARGET=rust vitest run",
    "test:watch": "vitest",
    "format": "prettier --write 'test/**/*.{ts,js,json}' 'bin/**/*.ts'"
  },
  "devDependencies": {
    "@facetlayer/subprocess": "^1.1.1",
    "@types/node": "^20.19.0",
    "expect-mcp": "0.8.0",
    "prettier": "3.6.2",
    "prettier-plugin-organize-imports": "4.3.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.2",
    "vitest": "^3.0.6"
  }
}
